{
    "version": "0.2.0",
    "configurations": [
        {
            "name": "Debug with LLDB",
            "type": "lldb",
            "request": "launch",
            "program": "${workspaceFolder}/build/room2_exe.exe",
            "args": [],
            "cwd": "${workspaceFolder}",
            "preLaunchTask": "CMake Build",
            "terminal": "integrated"
        }
    ]
}